/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
	images: {
		domains: ["lh3.googleusercontent.com"], // Add your image source domains here
	},
};

module.exports = nextConfig;
